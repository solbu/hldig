include @top_srcdir@/Makefile.config

OBJS=		htnotify.o 

LOCAL_DEFINES=  -DSENDMAIL=\"$(SENDMAIL)\"

TARGET=		htnotify

all:	$(TARGET)

$(TARGET):	$(OBJS) @top_srcdir@/htcommon/libcommon.a @top_srcdir@/htlib/libht.a
	$(CXX) -o $(TARGET) $(LDFLAGS) $(OBJS) $(LIBS)

install:	$(TARGET)
	$(INSTALL) $(TARGET) $(BIN_DIR)

clean:
	rm -f $(TARGET) $(OBJS) *~ *.bak *% a.out *.orig core

distclean:	clean
	rm -f Makefile *.proj

depend:
	$(MAKEDEPEND) @CPPFLAGS@ $(CXXFLAGS) -- $(OBJS)

htnotify.o:		@top_srcdir@/CONFIG
